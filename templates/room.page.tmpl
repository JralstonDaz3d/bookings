{{template "base" .}}

{{define "content"}}
    <div class="container my-5">
        <div class="row">
            <div class="column">
                <h1>Room page</h1>
                <p>Info about a single room and a way to reserve your room here.</p>
                <p>This came from the template [{{index .StringMap "test"}}]</p>
            </div>
        </div>
    </div>
{{end}}